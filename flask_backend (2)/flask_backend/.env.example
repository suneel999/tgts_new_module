# Copy to .env on the server (never commit .env)

FLASK_ENV=production
FLASK_DEBUG=False
SECRET_KEY=generate-a-long-random-string
JWT_SECRET_KEY=generate-another-long-random-string

# RDS MySQL (ap-south-1) — replace USER, PASSWORD, DATABASE_NAME; URL-encode password (@ → %40)
DATABASE_URL=mysql+pymysql://tgtsadmin:tgts1234@tgts-mysql-prod.ch4m6mm4g6qx.ap-south-1.rds.amazonaws.com:3306/DATABASE_NAME

PORT=5000
HOST=0.0.0.0

CORS_ORIGINS=https://admin.tgtccon2025.com
# Used for QR links etc.; origin is also merged into CORS allowlist automatically
ADMIN_FRONTEND_URL=https://admin.tgtccon2025.com

# Same host as the API but WITHOUT /api — used to prefix /uploads/... URLs in JSON responses
PUBLIC_BASE_URL=https://api.tgtccon2025.com

RATELIMIT_ENABLED=True
RATELIMIT_STORAGE_URL=memory://

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=ap-south-1
S3_BUCKET_NAME=tgts-media1
# false + valid AWS keys = uploads go to S3. true or missing keys = local disk only (/uploads/...)
S3_USE_LOCAL_STORAGE=false

MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=
MAIL_PASSWORD=

DEV_MODE=False
DEBUG_OTP=False
